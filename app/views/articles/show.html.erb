<h2><%= @article.title %> | <%= link_to "Edit", edit_article_path(@article.id) %> - <%= link_to "Delete",
            article_path(@article.id),
            method: :delete,
            data: { confirm: "Are you sure?" } %></li></h2>
<p><%= @article.content %></p>


<%= link_to "Back to list", articles_path %>
